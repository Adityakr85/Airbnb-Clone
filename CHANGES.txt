# Added complete Edit Listing workflow for hosts
# Reused Add Property page for editing existing listings
# Added backend API to fetch a single property for editing
# Added backend property update functionality
# Standardized property image handling across frontend and backend
# Fixed image rendering in:
  *  Global Cards
  *  Wishlist
  *  Property Details
  *  Image Gallery
  *  Trips
  *  Past Trips
  *  Booking Details
  *  Host Properties
  *  Admin Properties
# Fixed backend image path inconsistencies
# Updated booking/trip status to rely on backend status
# Separated active trips from completed trips
# Improved Property Details image gallery
# Added guests, bedrooms, and bathrooms to host property data
# Improved Host Properties UI with clickable cards and better image display
# Fixed Admin Properties image rendering and property data display

Implemented backend-driven Category Management system.

- Added category admin fields (icon, type, is_active, sort_order)
- Updated Category model with fillable fields and boolean casting
- Created CategoryController with CRUD operations
- Added enable/disable support for categories
- Added category reordering API
- Added public and admin category endpoints
- Integrated backend category API for Host Add Property page
- Categories are now dynamically managed from the admin panel

refactor(host): modularize Add Property flow and add amenities support

- Refactored Add Property into a dedicated folder with step-based components
- Split the multi-step form into separate files (01–09 + SuccessStep)
- Kept AddProperty.jsx as the parent controller for state, navigation, and submission
- Improved location step with integrated Leaflet map, search, draggable marker, and reverse geocoding
- Separated city (location) from full address handling
- Added backend amenities system with amenities and property_amenity tables
- Implemented many-to-many relationship between properties and amenities
- Added Amenity model and API endpoint for fetching active amenities
- Updated Property model with amenities relationship
- Enhanced PropertyController to validate, save, sync, and return amenities
- Updated property APIs to include amenities, description, address, guests, bedrooms, bathrooms, coordinates, category, images, and host details
- Prepared backend for dynamic amenities management and property details page




